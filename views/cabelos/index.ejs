<h1>Lista de cabelos</h1>

<!-- Formulário de filtro por categoria -->
<form method="GET" action="/cabelos">
    <div class="form-group">
        <label for="categoria">Filtrar por Categoria:</label>
        <select id="categoria" name="categoria" class="form-control">
            <option value="">Todas as Categorias</option>
            <% categorias.forEach(categoria => { %>
                <option value="<%= categoria.id %>" <%= categoriaSelecionada == categoria.id ? 'selected' : '' %>><%= categoria.nome %></option>
            <% }) %>
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Filtrar</button>
</form>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Preço</th>
            <th>Quantidade</th>
            <th>Categoria</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        <% cabelos.forEach(cabelo => { %>
            <tr>
                <td><%= cabelo.id %></td>
                <td><%= cabelo.nome %></td>
                <td><%= cabelo.descricao %></td>
                <td><%= cabelo.preco %></td>
                <td><%= cabelo.quantidade %></td>
                <td><%= cabelo.categoria_nome %></td>
                <td>
                    <a href="/cabelos/<%= cabelo.id %>" class="btn btn-primary">View</a>
                    <a href="/cabelos/<%= cabelo.id %>/edit" class="btn btn-warning">Edit</a>
                    <form action="/cabelos/<%= cabelo.id %>?_method=DELETE" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>
